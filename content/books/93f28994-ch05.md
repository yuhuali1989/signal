---
title: "大语言模型从入门到前沿 - 第5章: 多模态扩展：视觉、语音与代码"
book: "大语言模型从入门到前沿"
chapter: "5"
chapterTitle: "多模态扩展：视觉、语音与代码"
description: "系统梳理多模态 LLM 的三大理解架构（投影层/交叉注意力/原生多模态）、图像视频生成模型（UNet→DiT→FLUX/Sora/Kling）、以及理解与生成的融合趋势（Janus-Pro/Show-o/Transfusion）"
date: "2026-04-12"
updatedAt: "2026-04-28 15:00"
agent: "研究员→编辑→审校员"
tags:
  - "LLM"
  - "多模态"
  - "ViT"
  - "GPT-4o"
  - "代码生成"
type: "book"
---

# 第 5 章：多模态扩展——视觉、语音与代码

> 选自《大语言模型从入门到前沿》

## 5.1 多模态 LLM 的架构范式

多模态 LLM 的核心问题是：如何将视觉/音频等非文本信息编码为 LLM 能理解的 token 序列？

### 三种主流架构

```
方案 A: 编码器 + 投影层 + LLM（LLaVA 系列）
  Image → ViT Encoder → MLP Projector → LLM

方案 B: 原生多模态（GPT-4o / Gemini / Gemma 4）
  Image/Audio/Text → 统一 Tokenizer → 单一 Transformer

方案 C: 交叉注意力（Flamingo / Llama 3.2 / Llama 4）
  Image → ViT → Cross-Attention 注入到 LLM 层间
```

| 架构 | 代表模型 | 优点 | 缺点 |
|------|---------|------|------|
| 编码器+投影 | LLaVA, InternVL | 实现简单，可复用预训练 ViT | 模态融合不够深 |
| 原生多模态 | GPT-4o, Gemini, Gemma 4 | 统一架构，多模态生成 | 训练成本极高 |
| 交叉注意力 | Flamingo, Llama 3.2/4 | 融合更深，效果好 | 架构复杂度高 |

### 架构选型决策树

```python
def choose_multimodal_arch(budget, performance_req, modalities):
    """多模态架构选型指南"""
    if budget == "unlimited" and "generation" in performance_req:
        return "原生多模态"  # GPT-4o/Gemini: 最强但最贵
    
    if len(modalities) == 2 and "text" in modalities:
        if "vision" in modalities:
            return "编码器+投影"  # LLaVA: 最快上手
        if "audio" in modalities:
            return "编码器+适配器"  # Whisper + LLM
    
    if performance_req == "sota":
        return "交叉注意力"  # Flamingo/Llama 4: 最佳融合
    
    return "编码器+投影"  # 默认方案
```

### 5.1.1 Embedding 入门：文本、图像、音频的向量化对比

在进入具体架构前，许多读者会有一个朴素但关键的疑问：**文本 Embedding 是"查词表"得到向量（例如词表大小 65536，每个词分配一个 ID），那图像和音频既没有"词表"、也没有天然的"词"，它们的 Embedding 到底是怎么来的？**

这一节用一条主线把三种模态串起来：**所有 Embedding 的本质都是"把原始信号映射成一个 $d$ 维向量（例如 $d=4096$），只是获取向量的路径不同"**。

#### 1. 文本 Embedding：离散 ID → 查表

文本的 Embedding 有一个明显的"离散"特性，所以流程最直接：

```
原文: "你好"
  ↓ Tokenizer (BPE / SentencePiece)
Token ID: [872, 1234]        # 词表大小 V=65536
  ↓ nn.Embedding(V, d) 查表
向量:     [[0.12, -0.33, ..., 0.51],   # 4096 维
          [0.44,  0.02, ..., -0.08]]
```

关键点：
- 词表大小 $V$ 是固定的（例如 65536），Embedding 矩阵形状是 $V \times d$。
- 每个 token ID 对应矩阵的一行；"查表"就是 `embedding_table[id]`。
- 这个 Embedding 矩阵的参数是**训练出来的**，反向传播会更新每个 ID 对应的行。

```python
import torch.nn as nn
text_embed = nn.Embedding(num_embeddings=65536, embedding_dim=4096)
ids = torch.tensor([872, 1234])
x = text_embed(ids)           # shape: [2, 4096]
```

#### 2. 图像 Embedding：连续像素 → Patch → 线性投影

**图像没有天然的词表**（因为像素是连续值、而且数量爆炸：一张 $224 \times 224$ 的图有 50176 个像素，不可能像文本那样枚举一个有限词表）。所以图像 Embedding 采用完全不同的思路——**"切块 + 投影"**：

```
原图: [3, 224, 224]              # 3 通道，224×224 像素
  ↓ 切成 16×16 的 patch
Patches: 14×14 = 196 个 patch，每个 [3, 16, 16] = 768 个像素值
  ↓ Flatten 成 768 维向量
Vectors: [196, 768]
  ↓ 线性投影 nn.Linear(768, d)
Image Embedding: [196, 4096]     # 和文本 embedding 同维度 d=4096
```

关键对比：

| 方面 | 文本 | 图像 |
|------|------|------|
| 基本单位 | token（有限词表 65536） | patch（无限连续像素块） |
| 获取向量 | 查表（离散索引 → 行） | 线性投影（连续向量 → 连续向量） |
| 一条输入 | 几十~几千个 token | 固定 196/576 个 patch token |
| Embedding 矩阵 | $V \times d$ | $(P \times P \times 3) \times d$ |

**一句话**：图像没有 ID 这回事，它是用 `nn.Linear` 把 patch 的像素向量直接"压缩"成 $d$ 维向量；词表的等价物是"所有可能的 patch 组合"，但我们不枚举它，而是学一个线性函数来处理任意 patch。

```python
class PatchEmbed(nn.Module):
    def __init__(self, patch_size=16, in_ch=3, d=4096):
        super().__init__()
        # 用一个 Conv2d 同时完成"切 patch + 线性投影"
        self.proj = nn.Conv2d(in_ch, d, kernel_size=patch_size, stride=patch_size)
    def forward(self, img):             # img: [B, 3, 224, 224]
        x = self.proj(img)              # [B, d, 14, 14]
        x = x.flatten(2).transpose(1,2) # [B, 196, d]
        return x
```

这正是 **ViT（Vision Transformer）** 的第一步，后面 5.2 节会展开。

#### 3. 音频 Embedding：波形 → 频谱 → 两条路线

音频比图像还多一个"时间"维度。**原始音频是一维波形**（例如 16kHz 采样 1 秒钟 = 16000 个浮点数），太长且冗余，所以先要做"压缩表示"：

```
原始波形: [16000] 个浮点采样值（1 秒）
  ↓ STFT + Mel 滤波器组
Mel 频谱图: [80, 100]             # 80 个频带 × 100 个时间帧
                                   ← 到这里它就变成"一张图"了
```

接下来有**两条主流路线**，区别在于要不要离散化：

##### 路线 A：连续特征路线（Whisper / Qwen2-Audio，偏理解）

和图像思路几乎一样，把 Mel 频谱当成"图"来切 patch：

```
Mel 频谱 [80, 100]
  ↓ Conv1d / Patch 切块
Audio patches [T, 768]
  ↓ 线性投影
Audio Embedding [T, 4096]
```

特点：embedding 是**连续向量**，能喂给 LLM 做理解，但模型**无法直接生成音频**（因为输出是连续向量，难以还原波形）。

##### 路线 B：离散 Codebook 路线（Encodec / SoundStream，GPT-4o 采用，支持生成）

这条路线才是真正能让"音频有词表"的方案：用一个叫 **VQ-VAE（Vector Quantization VAE）** 的模型，把音频压缩成一串离散整数 ID，**像文本 token 一样**！

```
波形 → Encoder → 连续特征 z [T, 128]
                       ↓
                 在 Codebook（大小 K=1024）中找最近邻
                       ↓
              离散 token ID [T]  ← 每帧映射到 1024 个"音频码"之一
                       ↓
              nn.Embedding(1024, d) 查表
                       ↓
              Audio Embedding [T, 4096]
```

这样音频就"伪装"成了文本：原来的词表大小是 65536 个词，现在扩展为"65536 个词 + 1024 个音频码"，LLM 用**完全相同**的下一 token 预测机制同时处理文字和声音。GPT-4o 能在一个模型里又说话又唱歌，就是因为这条路线。

**三种 Embedding 对比总览**：

```mermaid
flowchart TB
    subgraph 文本
        T1[字符串 你好] --> T2[Tokenizer]
        T2 --> T3[Token ID: 872, 1234<br/>词表 V=65536]
        T3 --> T4[查表 nn.Embedding]
        T4 --> T5[向量 2, 4096]
    end
    subgraph 图像
        I1[像素张量 3, 224, 224] --> I2[切 patch 16x16]
        I2 --> I3[196 个 patch, 每个 768 维]
        I3 --> I4[线性投影 nn.Linear]
        I4 --> I5[向量 196, 4096]
    end
    subgraph 音频离散路线
        A1[波形 16000] --> A2[Mel 频谱 80, 100]
        A2 --> A3[VQ-VAE 编码 + 最近邻]
        A3 --> A4[Codebook ID, 1024 类]
        A4 --> A5[查表 nn.Embedding]
        A5 --> A6[向量 T, 4096]
    end
```

#### 4. 为什么最后 $d$ 都一样？

你可能已经注意到：三种 Embedding 最后都输出 $d=4096$（或 LLM 设定的任何维度）。这不是巧合——**它们必须维度一样，才能拼在同一条序列里送进 Transformer**：

```
输入序列 = [文本 token₁, 文本 token₂, 图像 patch₁, ..., 图像 patch₁₉₆, 音频码₁, ...]
         每个元素都是 4096 维向量
         ↓
      同一个 Transformer
```

这也是为什么 5.1 节所有架构（LLaVA / Flamingo / GPT-4o）本质都是"想办法把各模态变成 $d$ 维 token 序列"——**维度对齐是多模态融合的底线约定**。

> 记住这张总对比表，后续所有 ViT、SigLIP、Whisper、VQ-VAE 的细节都是在填充它的某一行。

| 模态 | 原始形态 | 中间表示 | 获取向量方式 | 是否有"词表" |
|------|---------|---------|------------|------------|
| 文本 | 字符串 | Token ID | 查 Embedding 表 | 有（BPE 词表 ~65K） |
| 图像 | 像素矩阵 | Patch 向量 | 线性投影 | 无（连续投影） |
| 音频（连续路线） | 波形 | Mel 频谱 Patch | 线性投影 | 无 |
| 音频（离散路线） | 波形 | Codebook ID | VQ-VAE 量化 + 查表 | 有（学出来的 ~1K 码） |

#### 5. 重要澄清：图像/音频其实都有"有词表"和"无词表"两条路线

上面的表容易让读者误以为"图像=无词表、音频=两条路线可选"。**事实上图像也有两条路线**，选哪条取决于**任务类型**：

```
任务类型         → 走哪条路线?
────────────────────────────────────────────────────────
看图说话（输入图/输出文字）  → 路线 A：连续 Patch（无词表）
  例：LLaVA、Qwen-VL、InternVL
  原理：图像只是"被理解"，模型最终输出的还是文本 token

文生图（输入文字/输出图）    → 路线 B：离散 VQ token（有词表）
  例：DALL-E 1、Parti、Chameleon、GPT-4o 生图
  原理：要让 Next Token Prediction 能预测图像，必须把图像离散化

统一多模态（输入输出都包含图） → 路线 B：离散 VQ token
  例：Chameleon、GPT-4o、Show-o
  原理：输入输出走同一套词表，才能无缝切换模态
```

**为什么"生成"任务必须离散化？**

因为 LLM 的输出层是 `Linear → Softmax`，Softmax 只能在**有限类别**上做归一化采样：

```python
logits = model(h)                # [V]  V 必须是有限整数
probs  = softmax(logits)         # 归一化到 V 个类别
next_id = sample(probs)          # 采样一个整数 ID
```

如果图像是**连续向量**（路线 A），模型根本没法做"下一 token 预测"——连续空间无限大，Softmax 不适用。所以一旦任务是"生成图像"，就必须先把图像离散化，这就是下一小节 VQ-VAE 存在的唯一理由。

**修正后的完整对比表**（记住这张，它覆盖了所有多模态场景）：

| 模态 | 路线 | 形态 | 获取方式 | 词表 | 能被 Next Token 预测？ |
|------|------|------|---------|------|--------------------|
| 文本 | 唯一 | 离散 | 查表 | ✅ BPE ~65K | ✅ |
| 图像 | **A：理解** | 连续向量 | 线性投影 | ❌ 无 | ❌ 只能被动输入 |
| 图像 | **B：生成** | **离散 ID** | **VQ-VAE 量化** | ✅ **~8K** | ✅ |
| 音频 | A：理解 | 连续向量 | 线性投影 | ❌ 无 | ❌ |
| 音频 | B：生成 | 离散 ID | VQ-VAE / Encodec | ✅ ~1K | ✅ |

> **一句话记忆**：**"要生成，必离散；要离散，用 VQ。"**

### 5.1.2 统一词表与模态路由：Next Token 如何区分模态？

上一节我们让图像和音频也"获得了词表"（通过 VQ-VAE），那紧接着的问题是：**在同一个 Transformer 里同时预测文字/图像/音频 token 时，模型怎么知道下一个 token 该属于哪种模态？**

答案非常朴素——**把所有模态的 token 塞进同一个大词表，用 ID 区间区分**。

#### 1. 统一词表的 ID 划分

```
统一词表示例（总大小 V = 75,840）:
├─ [0,     65535]  → 文本 token       （BPE 词表 65536）
├─ [65536, 73727]  → 图像 token       （VQ-VAE codebook 8192）
├─ [73728, 75775]  → 音频 token       （Encodec codebook 2048）
└─ [75776, 75839]  → 特殊控制 token   （<BOI>, <EOI>, <BOA>, <EOA>）
```

模型输出层 `Linear(d, V=75840)` 产生 logits，Softmax 采样得到一个 ID，看它落在哪个区间就知道是什么模态：

```python
def which_modality(token_id):
    if   token_id < 65536: return "text"
    elif token_id < 73728: return "image"
    elif token_id < 75776: return "audio"
    else:                  return "control"
```

这就是 **Meta Chameleon** 与 **GPT-4o** 采用的 **Early Fusion + Mixed-Modal Token** 思路。

#### 2. 模态分隔符：像 HTML 标签一样包裹内容

光有 ID 区间还不够——如果模型连续输出一张图（1024 个 token），怎么知道"画完了、该切回文字"？答案是**特殊控制 token**：

```
用户: "画一只猫，然后描述它"
模型输出序列:
  <BOI>                       ← Begin of Image，模态切换信号
  65700, 66012, 71234, ...    ← 1024 个图像 token（= 32×32 patch）
  <EOI>                       ← End of Image
  "这" "是" "一" "只"            ← 切回文字
  "橘" "色" "的" "猫"
  <EOS>                       ← 结束
```

关键：`<BOI>/<EOI>/<BOA>/<EOA>` 都是**普通 token**，只是 ID 预留在词表尾部。模型在训练时见过大量带这些标签的数据，**自动学会**"看到 `<BOI>` 后连续采样图像 token，直到采到 `<EOI>` 停止"。

#### 3. 采样时的模态约束

为了防止"生成图像中间串出一个文字 token"导致图片损坏，工程上有三种常用方案：

**方案 A：Logits Masking（最常用）**——在已知当前处于图像段时，把非图像 token 的 logits 设为 `-inf`：

```python
def masked_sample(logits, state):
    if state == "generating_image":
        mask = torch.full_like(logits, -float('inf'))
        mask[65536:73728] = 0          # 图像区间放行
        mask[EOI_TOKEN_ID] = 0         # 允许结束
        logits = logits + mask
    return sample(softmax(logits))
```

状态机由 `<BOI>/<EOI>` 等触发切换。

**方案 B：多头输出（Chameleon 原版）**——给每个模态配独立分类头，先用一个"路由头"决定模态，再用对应头采样。

**方案 C：纯数据驱动（GPT-4o 最激进）**——不加任何掩码，靠海量训练数据让模型自己学会模态边界。这依赖超大规模预训练，普通团队复现不了。

#### 4. 训练 Loss：所有模态共用一个 Cross-Entropy

```python
# labels: [B, L]，每个位置是 0~75839 的 ID（不管是文本/图像/音频）
logits = model(inputs_embeds)             # [B, L, 75840]
loss = F.cross_entropy(
    logits.view(-1, 75840),
    labels.view(-1),
    ignore_index=-100                     # 占位符位置不算 loss
)
```

反向传播时，图像 token 的梯度只会更新词表中 `[65536, 73728]` 那 8192 行 embedding，文本 token 只更新前 65536 行——**互不干扰，又共享底层 Transformer**。

#### 5. 三种主流模型的路由策略对比

| 模型 | 统一词表 | 分隔符 | 采样约束 | 图像生成方式 |
|------|---------|-------|---------|------------|
| **Chameleon**（Meta 2024） | ✅（文+图） | `<BOI>/<EOI>` | Logits Masking | VQ-VAE 离散 token |
| **GPT-4o**（OpenAI 2024） | ✅（文+图+音） | 特殊标记 | 纯数据驱动 | 未公开（推测离散） |
| **Transfusion**（Meta 2024） | ❌（文离散+图连续） | `<BOI>/<EOI>` | 状态机切换 | **扩散生成连续 latent** |
| **Janus-Pro**（DeepSeek 2025） | 部分共享 | `<BOI>/<EOI>` | 双编码器分离 | VQ 离散 token |

> **⚠️ 特例：Transfusion** 证明了"不是所有模态都必须变成离散 token"。文本部分用 Next Token Prediction + Softmax，图像部分用扩散模型直接预测连续 latent，两种 loss 在同一个 Transformer 里联合训练。详见 5.9 节。

### 5.1.3 VQ-VAE 深入：视觉/音频词表是如何"训练"出来的？

上面两节多次提到"VQ-VAE codebook 有 8192 个码"，但这个 codebook 是**凭空指定**的吗？显然不是。这一节回答你应该最关心的问题：**图像/音频的词表到底是怎么来的？它里面每一项长什么样？怎么训练？**

#### 1. 核心思想：把连续特征"四舍五入"到有限个代表向量

VQ-VAE（Vector Quantization VAE）的本质是一个**带"离散中转站"的自编码器**：

```
                       ┌──────── Codebook ────────┐
                       │  e₁ = [ 0.12, -0.33, ...] │  ← 8192 个 128 维向量
                       │  e₂ = [-0.44,  0.22, ...] │    每一个就是一个"视觉字根"
                       │  e₃ = [ 0.81,  0.05, ...] │
                       │  ...                      │
                       │  e₈₁₉₂ = [...]            │
                       └─────────────────────────┘
                                    ↑
                                    │ 最近邻查找（核心步骤）
                                    │
原图 x ──▶ Encoder ──▶ 连续特征 z_e(x) ──▶ 离散 ID q ──▶ 查表 z_q ──▶ Decoder ──▶ 重建 x̂
         (CNN)       [32×32×128]       [32×32 整数]   [32×32×128]    (CNN)
```

每个空间位置的 128 维连续向量 $z_e$，会被**替换**为 codebook 中距离它最近的那个向量 $e_k$，并记录 $k$ 这个整数 ID。最后留下来的就是一个 32×32 的整数矩阵——这就是图像的"token 序列"。

**最近邻查找的数学定义**：

$$q(z_e) = \arg\min_{k \in \{1,\dots,K\}} \|z_e - e_k\|_2$$

代码实现：

```python
def quantize(z_e, codebook):
    # z_e:      [B, H, W, D]   连续特征
    # codebook: [K, D]          K=8192, D=128
    # 计算每个位置到所有 code 的欧氏距离
    distances = (z_e.unsqueeze(-2) - codebook).pow(2).sum(-1)  # [B, H, W, K]
    ids = distances.argmin(-1)                                 # [B, H, W]
    z_q = codebook[ids]                                        # [B, H, W, D]
    return ids, z_q
```

#### 2. Codebook 是怎么训练出来的？

Codebook（`nn.Embedding(8192, 128)`）**是一个可学习的参数矩阵**，初始化时是随机的，训练过程中和 Encoder/Decoder 一起被优化。训练目标是三项 Loss 之和：

$$\mathcal{L} = \underbrace{\|x - \hat{x}\|^2}_{\text{重建 Loss}} + \underbrace{\|\text{sg}[z_e] - e_k\|^2}_{\text{Codebook Loss}} + \beta \underbrace{\|z_e - \text{sg}[e_k]\|^2}_{\text{Commitment Loss}}$$

其中 `sg[·]` 是 **stop gradient**（反向传播时截断梯度）。三项 Loss 分工：

| Loss 项 | 作用 | 更新谁 |
|---------|------|--------|
| **重建 Loss** | 保证 Decoder 能还原出原图 | Encoder + Decoder |
| **Codebook Loss** | 把被选中的 code $e_k$ 向 $z_e$ 靠近 | **只更新 Codebook** |
| **Commitment Loss** | 防止 Encoder 输出到处跑，强制它靠近某个 code | **只更新 Encoder** |

**直觉**：Codebook 就像一堆"磁铁"，散落在 128 维特征空间里。训练时每个 Encoder 输出都会吸引**离它最近的那块磁铁**向自己靠拢（Codebook Loss），同时 Encoder 也被那块磁铁吸回去（Commitment Loss）。久而久之，8192 个磁铁会**自动分布到数据的 8192 个"代表性模式"上**——比如某个 code 专门代表"毛茸茸的纹理"，另一个代表"蓝天背景"，再一个代表"眼睛"……

#### 3. 直通估计器（Straight-Through Estimator）：argmin 不可导怎么办？

$\arg\min$ 是离散操作，没有梯度。VQ-VAE 用一个巧妙的技巧绕过：

```python
# 前向：用量化后的 z_q
# 反向：梯度直接"穿透"回 z_e（假装 z_q = z_e）
z_q = z_e + (z_q - z_e).detach()
```

这样 Decoder 算出的梯度能流回 Encoder，就像量化从未发生过——这就是 **Straight-Through Estimator**。

#### 4. 训练完毕后，codebook 的每一项"长什么样"？

把每个 code $e_k$ 通过 Decoder 还原到图像空间，就能**可视化它代表的视觉概念**：

```
Codebook 可视化（示意）:
e₁    → 红色纯色块
e₁₂₃  → 斜向条纹纹理
e₄₅₆  → 眼睛形状
e₂₀₀₀ → 毛发细节
e₇₀₀₀ → 天空渐变
...
```

**所以图像"词表"里的每一项，本质上是一块可复用的视觉基元**。任何一张图都可以用这 8192 个基元的**空间组合**来近似还原——就像汉字用 500 个偏旁部首就能拼出几万个字。

#### 5. 音频 Codebook：完全一样的套路

音频的 VQ-VAE（Encodec、SoundStream）原理**一模一样**，只是把 2D CNN 换成 1D：

```
原始波形 [16000]
  ↓ 1D Conv Encoder（降采样 320 倍）
连续特征 [50, 128]
  ↓ 最近邻量化（Codebook K=1024）
离散 token [50]
  ↓ 查表 + 1D Conv Decoder
重建波形 [16000]
```

实际应用中还会用 **Residual VQ**（RVQ）——堆叠多个 codebook 做残差量化，每个 codebook 修正上一个的误差。Encodec 用 8 层 RVQ × 1024 码 = 有效词表约 $1024^8$，极大提升重建质量。

#### 6. 还原度的痛点：为什么早期音频 VQ 不能用？Encodec 怎么解决的？

一个常见的专业质疑：**"VQ-VAE 把音频压成离散 token，还原度不是很差吗？怎么敢用它做生成？"** 这个疑问非常准确——早期单层 VQ-VAE 的音频确实"一听就是机器人声"，语音离散化技术正是在解决这个痛点的过程中一路演进到今天的。

##### 为什么音频比图像更难还原？

| 维度 | 图像 | 音频 |
|------|------|------|
| 人类感知敏感度 | 对像素误差宽容（模糊几度看不出） | **对相位/频率极其敏感**（偏几 Hz 就跑调） |
| 信息冗余 | 空间冗余大（大片天空都是蓝色） | 时间冗余小（每个采样点都携带信息） |
| 失真表现 | 看起来"有点糊" | 听起来像**金属音、机器人声** |
| 采样规模 | 一张图 224×224 ≈ 5 万点 | 1 秒 16kHz = 16000 点 |

单层 VQ-VAE 把 16000 个采样点压到几十个 token，信息损失极大 → 还原出来就是典型的"AI 机械音"。

##### 音频离散编码的演进路线

```
2017  原始 VQ-VAE                     → 明显机械音，仅能做概念验证
 │    单层 codebook，码率太低
 ▼
2019  VQ-VAE-2                       → 多尺度改善，但仍不可用于产品
 ▼
2021  SoundStream (Google)           → 首次达到"可接受"音质
 │    关键创新：Residual VQ (RVQ)
 ▼
2022  Encodec (Meta)                 → 接近透明音质，6 kbps 即可
 │    RVQ + 多判别器对抗 + 感知损失
 ▼
2023  DAC (Descript)                 → 44.1kHz 高保真，音乐级
 ▼
2024  Mimi (Moshi 用)、SNAC          → 超低码率 SOTA（1.1 kbps 仍可懂）
```

##### Encodec 的三招组合拳

**① Residual VQ（解决码率-质量权衡）**

单层 VQ 的问题是"一个 code 要代表整段特征，信息量不够"。RVQ 像泰勒展开一样**分层残差修正**：

```
连续特征 z
 ├─► Codebook 1 → 最近的 e₁ → 残差 r₁ = z - e₁
 ├─► Codebook 2 → 最近的 e₂ → 残差 r₂ = r₁ - e₂
 ├─► ...
 └─► Codebook 8 → 最近的 e₈

最终重建：ẑ = e₁ + e₂ + ... + e₈
```

```python
def residual_vq(z, codebooks):          # codebooks: 8 个 [1024, 128]
    z_q_total, residual, ids = 0, z, []
    for cb in codebooks:
        dist = (residual.unsqueeze(-2) - cb).pow(2).sum(-1)
        idx = dist.argmin(-1)
        e_k = cb[idx]
        z_q_total = z_q_total + e_k     # 累加
        residual  = residual - e_k      # 剩下的交给下一层
        ids.append(idx)
    return z_q_total, ids               # 返回 8 个 token 序列
```

等价于把 $10^{24}$ 大小的超大词表**分解为 8 个 1024 的小词表**，既保证表达能力，又避免训一个天文数字大小 codebook 的灾难。

**② 多判别器对抗训练（解决"听起来像机器人"）**

纯 L2 重建 Loss 只让波形"数值接近"，但人耳感知的是**频谱结构**。Encodec 引入三种判别器协同对抗：

```
Generator (Decoder) ←── 对抗 ──→ 多判别器
                                 ├─ MS-STFT D（多分辨率频谱判别器）
                                 ├─ MP-D（多周期判别器，抓音高）
                                 └─ MS-D（多尺度波形判别器）
```

对抗 Loss 强迫生成器产生"听起来真实"的声音，而非"数值对但听感假"。

**③ 多项联合损失**

```python
loss_total = λ₁ * L1(waveform, waveform_hat)           # 时域
           + λ₂ * L1(mel(waveform), mel(waveform_hat)) # 频域（感知）
           + λ₃ * adversarial_loss                      # 对抗
           + λ₄ * feature_matching                      # 判别器中间层匹配
           + codebook_loss + commitment_loss            # VQ 标准项
```

##### 当前还原度水平（2025 实测 MOS）

| 模型 | 比特率 | 主观 MOS（满分 5） | 备注 |
|------|--------|-----------------|------|
| 原始 24kHz 波形 | 384 kbps | 5.0 | 无损参考 |
| **Encodec** | 6 kbps | **4.0~4.2** | 接近透明，**语音完全可用** |
| **DAC** | 8 kbps (44.1kHz) | **4.3~4.5** | 音乐级保真 |
| **Mimi**（Moshi） | 1.1 kbps | 3.8 | 超低码率仍可懂 |
| 早期 SoundStream | 3 kbps | 3.5 | 明显失真 |
| 2019 VQ-VAE-2 | — | 2.5 | 机械感明显 |

**结论**：**语音还原度问题在 2022 年 Encodec 之后基本解决**，到 DAC/Mimi 时代已达到"盲测难分辨"水平。这也是为什么 GPT-4o、Moshi 这一代模型才真正敢用离散 token 做端到端语音生成——**没有 Encodec 级别的 codec，就没有今天的语音 LLM**。

##### 仍未完全解决的三个残留问题

1. **极低码率下的情感丢失**：Moshi Mimi 为实时对话压到 1.1 kbps，语气、情感细节会损失，所以 Moshi 说话略显平淡。
2. **音乐比语音更难**：语音主要频率集中在 200-4000Hz，音乐跨度 20Hz-20kHz 且多音源混叠，**音乐级离散编码仍是难点**，DAC 也未达无损。
3. **长序列误差累积**：生成 10 秒音频要预测数百 token，一个采样错误会连锁偏离（类似文本幻觉，但听感更刺耳）。这正是 Consistency Model、Flow Matching 继续在音频生成领域探索的原因。

> **回到那个质疑**：是的，早期音频 VQ 还原度差，但 **RVQ + 对抗训练 + 感知损失**这套组合拳已经把语音推到"可生产"水平。"离散化会不会损失音质"这个 2019 年还成立的批评，2025 年看已经过时了。

#### 7. 对比总结：三种"词表"的来源差异

| 词表类型 | 来源 | 是否需要单独训练？ | 词表大小 | 每项代表什么 |
|---------|------|------------------|---------|------------|
| 文本 BPE | 统计语料中高频字节对 | ❌ 一次性离线构建 | ~65K | 一个字符或子词 |
| 图像 VQ Codebook | VQ-VAE 和重建任务**联合训练** | ✅ 需要单独训 VAE | ~8K | 一块视觉基元 |
| 音频 Encodec Codebook | VQ-VAE + 对抗训练 | ✅ 需要单独训 VAE | ~1K×8 层 | 一段声学基元 |

> **核心结论**：**文本词表是"数出来"的（频率统计），图像/音频词表是"学出来"的（端到端训练）**。这是多模态相比纯文本最大的工程成本——在训 LLM 之前，你得先训好一个 VQ-VAE。

铺垫完成，下面正式进入视觉编码器的工程细节。

## 5.2 视觉编码器：从 ViT 到 SigLIP

视觉 Transformer（ViT）将图像切成 patch 后作为序列输入：

$$x = [x_{cls}; \; x_{p_1} E; \; x_{p_2} E; \; \dots; \; x_{p_N} E] + E_{pos}$$

其中 patch 大小通常为 $14 \times 14$ 或 $16 \times 16$，一张 $336 \times 336$ 的图像产生 576 个视觉 token。

### 视觉编码器演进

| 编码器 | 年份 | 特点 | 代表模型 |
|--------|:---:|------|---------|
| ViT-L | 2020 | 标准 ViT，ImageNet 预训练 | 早期 LLaVA |
| CLIP ViT | 2021 | 文本-图像对比学习，语义对齐好 | LLaVA-1.5 |
| SigLIP | 2023 | Sigmoid loss 替代 Softmax，不需要全局 batch | InternVL2, PaLI |
| DINOv2 | 2023 | 自监督学习，细粒度特征强 | 分割/检测任务 |
| NaViT | 2024 | 原生可变分辨率，无需 resize | Gemini 1.5+ |
| AIMv2 | 2025 | Apple 自研，端侧优化 | Apple Intelligence |

### SigLIP vs CLIP 的关键差异

```python
# CLIP: Softmax 对比损失（需要全局 batch 同步）
def clip_loss(image_emb, text_emb, temperature):
    # 需要 all_gather 收集全局 batch
    logits = image_emb @ text_emb.T / temperature  # [B, B]
    labels = torch.arange(B)
    loss = (F.cross_entropy(logits, labels) + 
            F.cross_entropy(logits.T, labels)) / 2
    return loss

# SigLIP: Sigmoid 二元损失（单 GPU 即可）
def siglip_loss(image_emb, text_emb, temperature, bias):
    logits = image_emb @ text_emb.T * temperature + bias  # [B, B]
    labels = 2 * torch.eye(B) - 1  # +1 for pairs, -1 for non-pairs
    loss = -F.logsigmoid(labels * logits).mean()
    return loss
```

SigLIP 的优势：
1. **无需全局 batch**：每个 GPU 独立计算，训练效率提升 2-3x
2. **更好的零样本迁移**：在 27 个评测上平均超越 CLIP
3. **灵活的分辨率**：配合 NaViT 支持任意输入尺寸

### 动态分辨率（LLaVA-NeXT / InternVL2）

支持任意分辨率输入，通过切分 + 缩略图组合：

```
高分辨率图片 (1344×896)
  → 切分为 2×3 = 6 个子图 (448×448 each)
  → 每个子图 ViT 编码 → 576 tokens
  → 总计 6 × 576 + 576(缩略图) = 4032 visual tokens
```

### 视觉 Token 压缩技术

4032 个视觉 token 会严重消耗上下文窗口。主流压缩方案：

| 方案 | 压缩比 | 方法 | 代表 |
|------|:---:|------|------|
| Perceiver Resampler | 4-16x | 可学习查询 + 交叉注意力 | Flamingo, Qwen-VL |
| Spatial Pooling | 2-4x | 平均池化/卷积降采样 | LLaVA-1.6 |
| Token Merging (ToMe) | 2-4x | 相似 token 合并 | InternVL2 |
| Q-Former | 8-16x | 查询 Transformer | BLIP-2 |

## 5.3 视觉-语言对齐训练

### 三阶段训练策略（2026 最佳实践）

**Stage 1: 预训练投影层**
- 冻结 ViT 和 LLM，只训练投影层
- 数据：图文配对数据（如 LAION-5B）
- 目标：让投影层学会将视觉特征映射到语言空间
- 通常 1 epoch，数小时完成

**Stage 2: 高质量对齐微调**
- 解冻 LLM（或用 LoRA），冻结或解冻 ViT
- 数据：多模态指令数据（VQA、图表理解、OCR 等）
- 目标：赋予模型遵循多模态指令的能力
- 数据量：1-5M 样本

**Stage 3: 对齐与安全（RLHF/DPO for Vision）**
- 多模态偏好数据
- 减少幻觉（Hallucination）：模型描述图中不存在的内容
- 提升细粒度理解：空间关系、计数、文字识别

### 多模态幻觉问题

多模态模型的一个核心挑战是 **视觉幻觉**：

```python
# 典型幻觉场景
prompt = "这张图片中有几只猫？"
image = "一张有 2 只猫的图片"

# 幻觉回答: "图片中有 3 只猫，一只橘猫在沙发上..."
# 正确回答: "图片中有 2 只猫"

# 缓解方法
class HallucinationMitigation:
    def __init__(self):
        self.strategies = [
            "对比解码: 减少语言先验的影响",
            "RLHF-V: 基于细粒度标注的偏好学习",
            "视觉 Grounding: 回答与图像区域绑定",
            "自一致性: 多次采样+投票验证",
        ]
```

### 损失函数

$$\mathcal{L} = -\sum_{t \in \text{response}} \log P(x_t | x_{<t}, \text{visual\_tokens}; \theta)$$

## 5.4 语音模态：从 Whisper 到 GPT-4o

### 语音理解

语音 → 特征提取（Mel-spectrogram）→ 语音编码器（Whisper-style）→ LLM

```python
class SpeechUnderstanding:
    """语音理解 pipeline"""
    
    def __init__(self):
        self.feature_extractor = MelSpectrogram(
            sample_rate=16000,
            n_fft=400,
            n_mels=80,
            hop_length=160
        )
        self.encoder = WhisperEncoder()  # Transformer encoder
        self.projector = nn.Linear(1280, 4096)  # 映射到 LLM 维度
    
    def encode(self, audio_waveform):
        # 30 秒音频 → 80x3000 Mel 特征 → 1500 个语音 token
        mel = self.feature_extractor(audio_waveform)
        speech_features = self.encoder(mel)
        speech_tokens = self.projector(speech_features)
        return speech_tokens
```

### 语音生成（Speech Synthesis）

GPT-4o 实现了**端到端语音生成**，无需 TTS 管线：

$$\text{Text/Audio Input} \rightarrow \text{Unified Model} \rightarrow \text{Audio Tokens} \rightarrow \text{Vocoder} \rightarrow \text{Waveform}$$

关键突破：GPT-4o 能实时理解说话人的语气、情绪，并以适当的语调回应，延迟 < 300ms。

### 语音 Token 化方案对比

| 方案 | 编码器 | Token 率 | 特点 |
|------|--------|:---:|------|
| Whisper-style | Transformer | 50 tok/s | 语义丰富，语音理解强 |
| SpeechTokenizer | RVQ (残差量化) | 50-75 tok/s | 保留声学细节，可生成 |
| Encodec (Meta) | Neural Codec | 24-75 tok/s | 高压缩比，用于 MusicGen |
| USM (Google) | Conformer | 25 tok/s | 支持 300+ 语言 |
| VoxCPM2 (2026) | Tokenizer-free | N/A | 跳过离散化，连续表征直接输入 |

### 端到端语音对话的架构演进

```
2023: ASR → LLM → TTS（级联，延迟 2-5s）
2024: GPT-4o 统一模型（端到端，延迟 300ms）
2025: Moshi/SpiritLM 开源全双工（可打断，延迟 200ms）
2026: VoxCPM2 无 Tokenizer（连续语音表征，延迟 150ms）
```

## 5.5 代码能力：从 Codex 到 AI 编程 Agent

代码是一种特殊的"语言"，LLM 在代码上的能力来自三大要素：

### 5.5.1 预训练数据中的代码配比

```python
# 主流模型的代码数据占比
code_ratios = {
    "GPT-4":      "~8-12%（估计）",
    "Llama 3.1":  "17%（官方公布）",
    "DeepSeek-V3": "25%（代码+数学强化）",
    "Llama 4":    "20%（含代码推理数据）",
    "StarCoder2":  "100%（纯代码模型）",
}
# 经验规律：代码占比 15-25% 时通用能力和代码能力平衡最优
```

### 5.5.2 FIM（Fill-in-the-Middle）训练

```
原始代码:  def add(a, b): return a + b
FIM 格式:  <PRE>def add(a, b):<SUF>+ b<MID> return a <EOT>
```

FIM 使模型能够在给定上下文和后文的情况下填充中间代码，这是代码补全的核心能力。

### 5.5.3 代码推理（Code Reasoning）

2025-2026 年的新趋势：不只是生成代码，还要**推理代码**：

```
传统代码模型: 输入 → 直接生成代码
代码推理模型: 输入 → 分析问题 → 制定方案 → 生成代码 → 验证

DeepSeek-R1 代码推理过程:
<think>
1. 这个问题需要实现二叉树的最大路径和
2. 路径可以不经过根节点
3. 使用递归：每个节点计算经过它的最大路径
4. 全局变量维护最大值
5. 注意负数节点的处理
</think>

def maxPathSum(self, root):
    self.max_sum = float('-inf')
    def dfs(node):
        if not node: return 0
        left = max(dfs(node.left), 0)   # 负数就不选
        right = max(dfs(node.right), 0)
        self.max_sum = max(self.max_sum, left + right + node.val)
        return max(left, right) + node.val
    dfs(root)
    return self.max_sum
```

### 5.5.4 代码评测基准（2026 更新）

| 基准 | 描述 | GPT-5.4 | Claude Opus 4.6 | DeepSeek-V4 | Llama 4 |
|------|------|:---:|:---:|:---:|:---:|
| HumanEval+ | 函数级代码生成 | 93.2% | 92.7% | 91.5% | 94.8% |
| SWE-Bench Verified | 真实 Issue 修复 | 57.8% | 58.9% | 52.3% | 61.3% |
| MBPP+ | 编程基础题 | 89.4% | 88.1% | 87.3% | 90.2% |
| LiveCodeBench | 竞赛编程 (实时) | 42.3% | 38.7% | 45.1% | 41.8% |
| BigCodeBench | 复杂库调用 | 72.1% | 71.5% | 69.8% | 73.4% |

### 5.5.5 从代码生成到编程 Agent

2026 年代码能力的最大变化：从单一的代码生成演进为**自主编程 Agent**：

```
2021-2023: Copilot / ChatGPT → 代码补全/问答
2024: Cursor / Devin → IDE 集成编码助手
2025: Claude Code / Codex Agent → 终端原生 Agent
2026: Claude Code Agent → 自主 repo 理解 + 多文件编辑 + CI 集成
     SWE-Bench 从 38% (2024) → 68% (2026)
```

## 5.6 Gemma 4：开源原生多模态的里程碑

Google 2026 年 4 月发布的 Gemma 4 是开源原生多模态的标杆：

### 架构创新

```
Gemma 4 架构:
├── 文本: SentencePiece tokenizer (256K vocab)
├── 视觉: SigLIP-400M → 投影层 → Transformer
├── 音频: USM Encoder → 投影层 → Transformer
├── 注意力: GQA + RoPE (2D for vision)
└── 所有模态在同一 Transformer 中联合训练
```

### 四款变体

| 变体 | 参数量 | 类型 | 特点 |
|------|:---:|:---:|------|
| Gemma 4 27B | 27B | Dense | 旗舰通用模型 |
| Gemma 4 26B-MoE | 26B (6B active) | MoE | 效率最优 |
| Gemma 4 4B | 4B | Dense | 中端设备 |
| Gemma 4 E2B | 2B | Dense | 手机/IoT |

E2B 变体在手机上的推理速度：

```
设备: Pixel 9 Pro (Tensor G5)
模型: Gemma 4 E2B (INT4 量化)
推理速度: 42 tokens/sec
首 token 延迟: 180ms
内存占用: 1.8 GB
```

## 5.7 多模态理解模型：主流架构深度解析

多模态**理解**模型的核心任务是：给定图像/视频/音频，回答问题、描述内容、推理关系。2026 年已形成三大主流架构流派。

### 5.7.1 流派一：编码器 + 投影层（Projector-based）

这是最主流的工程路线，代表作 LLaVA 系列和 InternVL 系列。

**核心思路**：用预训练好的视觉编码器（ViT/SigLIP）提取视觉特征，通过一个轻量投影层（MLP 或 Q-Former）将视觉 token 映射到 LLM 的词嵌入空间，然后拼接文本 token 一起送入 LLM。

```
┌─────────────────────────────────────────────────────────────┐
│                  Projector-based 架构                        │
│                                                             │
│  Image ──→ [ViT/SigLIP] ──→ Visual Features                │
│                                    │                        │
│                              [MLP Projector]                │
│                                    │                        │
│  Text ──→ [Tokenizer] ──→ Text Tokens                      │
│                                    │                        │
│              [Visual Tokens ⊕ Text Tokens] ──→ [LLM] ──→ 答案│
└─────────────────────────────────────────────────────────────┘
```

**LLaVA 系列演进**：

| 版本 | 投影层 | 视觉编码器 | 关键改进 |
|------|--------|-----------|---------|
| LLaVA-1.0 | 单层 Linear | CLIP ViT-L | 概念验证，仅 595K 对齐数据 |
| LLaVA-1.5 | 2层 MLP | CLIP ViT-L/336px | MLP 替代 Linear，效果大幅提升 |
| LLaVA-NeXT | 2层 MLP | CLIP ViT-L | 动态高分辨率（最高 4 tiles） |
| LLaVA-OneVision | 2层 MLP | SigLIP-SO400M | 单图/多图/视频统一，SOTA |

```python
# LLaVA-1.5 投影层实现（核心就是两层 MLP）
class LLaVAProjector(nn.Module):
    def __init__(self, vision_dim=1024, llm_dim=4096):
        super().__init__()
        self.proj = nn.Sequential(
            nn.Linear(vision_dim, llm_dim),
            nn.GELU(),
            nn.Linear(llm_dim, llm_dim),
        )
    
    def forward(self, visual_features):
        # visual_features: [B, N_patches, vision_dim]
        # 输出: [B, N_patches, llm_dim]，直接拼接到文本 token 前
        return self.proj(visual_features)

# 前向传播：视觉 token 插入文本序列
def llava_forward(image, text_tokens, model):
    # 1. 视觉编码
    visual_feats = model.vision_encoder(image)  # [B, 576, 1024]
    visual_tokens = model.projector(visual_feats)  # [B, 576, 4096]
    
    # 2. 找到 <image> 占位符位置，替换为视觉 token
    img_pos = (text_tokens == IMAGE_TOKEN_ID).nonzero()
    input_embeds = model.embed_tokens(text_tokens)
    input_embeds[img_pos] = visual_tokens.reshape(-1, 4096)
    
    # 3. LLM 自回归生成
    return model.llm(inputs_embeds=input_embeds)
```

**InternVL2 的关键创新**：

InternVL2 是 2025 年开源多模态理解的最强模型之一，核心创新在于：

1. **InternViT-6B**：自研 60 亿参数视觉编码器，远超 CLIP ViT-L（307M）
2. **动态分辨率 + Pixel Shuffle**：将 4×4 的视觉 token 合并为 1 个，压缩 16 倍
3. **渐进式训练**：先对齐视觉编码器，再联合微调

```python
# InternVL2 的 Pixel Shuffle 压缩（4x4 → 1 token）
def pixel_shuffle(visual_tokens, scale_factor=0.5):
    """
    将相邻的 4 个 token 合并为 1 个（空间降采样）
    visual_tokens: [B, H*W, C] → [B, H/2*W/2, C*4]
    """
    B, N, C = visual_tokens.shape
    H = W = int(N ** 0.5)
    x = visual_tokens.reshape(B, H, W, C)
    # 2x2 空间合并
    x = x.reshape(B, H//2, 2, W//2, 2, C)
    x = x.permute(0, 1, 3, 2, 4, 5).reshape(B, H//2 * W//2, C * 4)
    return x  # token 数量减少 4 倍，维度增加 4 倍
```

### 5.7.2 流派二：交叉注意力注入（Cross-Attention Injection）

代表作：Flamingo（DeepMind）、Llama 3.2 Vision、Llama 4。

**核心思路**：不把视觉 token 拼接到文本序列，而是在 LLM 的每隔 N 层插入一个**交叉注意力层**，让文本 token 主动"查询"视觉特征。

```
┌─────────────────────────────────────────────────────────────┐
│                  Cross-Attention 架构（Flamingo）             │
│                                                             │
│  Image ──→ [ViT] ──→ Visual Features (K, V)                │
│                              │                              │
│  Text ──→ [LLM Layer 1] ──→ [LLM Layer 2]                 │
│                                    │                        │
│                         [Cross-Attn: Q=text, K/V=visual]   │
│                                    │                        │
│                         [LLM Layer 3] ──→ ... ──→ 答案      │
└─────────────────────────────────────────────────────────────┘
```

```python
class FlamingoLayer(nn.Module):
    """Flamingo 的门控交叉注意力层"""
    def __init__(self, d_model, n_heads):
        super().__init__()
        self.cross_attn = nn.MultiheadAttention(d_model, n_heads)
        self.tanh_gate = nn.Parameter(torch.zeros(1))  # 初始为 0，不影响原始 LLM
        self.norm = nn.LayerNorm(d_model)
    
    def forward(self, text_hidden, visual_features):
        # text_hidden: [L_text, B, D]（文本 token 的隐状态）
        # visual_features: [L_vis, B, D]（视觉特征）
        
        # 交叉注意力：文本 query 视觉 key/value
        attn_out, _ = self.cross_attn(
            query=text_hidden,
            key=visual_features,
            value=visual_features
        )
        # 门控：tanh(gate) 初始为 0，训练中逐渐开放
        return text_hidden + self.tanh_gate.tanh() * self.norm(attn_out)
```

**Flamingo vs LLaVA 的本质区别**：

| 维度 | LLaVA（投影层） | Flamingo（交叉注意力） |
|------|---------------|---------------------|
| 视觉融合深度 | 浅（只在输入层） | 深（每隔 N 层都融合） |
| 多图支持 | 困难（上下文爆炸） | 原生支持（交叉注意力天然多图） |
| LLM 冻结 | 通常解冻 | 可完全冻结（门控初始为 0） |
| 训练效率 | 高（参数少） | 低（需要额外交叉注意力层） |
| 视频理解 | 需要特殊处理 | 天然支持（时序帧作为多图） |

**Llama 4 的 Early Fusion**：

Llama 4 进一步演进，采用 **Early Fusion**——在 Embedding 层就将视觉 token 和文本 token 统一编码，而非在中间层注入：

```python
# Llama 4 Early Fusion：视觉 token 和文本 token 在同一序列中
class Llama4EarlyFusion(nn.Module):
    def __init__(self):
        self.text_embed = nn.Embedding(vocab_size, d_model)
        self.vision_encoder = SigLIP()
        self.vision_projector = nn.Linear(vision_dim, d_model)
        # 视觉 token 和文本 token 共享同一个 Transformer
        self.transformer = LlamaTransformer()
    
    def forward(self, text_ids, images):
        # 文本 embedding
        text_embeds = self.text_embed(text_ids)  # [B, L_text, D]
        
        # 视觉 embedding（投影到同一维度）
        vis_feats = self.vision_encoder(images)
        vis_embeds = self.vision_projector(vis_feats)  # [B, L_vis, D]
        
        # 统一序列：[vis_tokens, text_tokens]
        combined = torch.cat([vis_embeds, text_embeds], dim=1)
        
        # 单一 Transformer 处理所有模态
        return self.transformer(combined)
```

### 5.7.3 流派三：原生多模态（Native Multimodal）

代表作：GPT-4o、Gemini 1.5/2.0、Gemma 4。

**核心思路**：从预训练阶段就将所有模态的 token 混合在一起训练，没有独立的"视觉编码器"和"投影层"，所有模态共享同一个 Tokenizer 和 Transformer。

```
┌─────────────────────────────────────────────────────────────┐
│                  原生多模态架构（Gemini 风格）                  │
│                                                             │
│  Image ──→ [Patch Tokenizer] ──→ Image Tokens              │
│  Audio ──→ [Spectrogram Tokenizer] ──→ Audio Tokens        │
│  Text  ──→ [SentencePiece] ──→ Text Tokens                 │
│                                                             │
│  [Image Tokens ⊕ Audio Tokens ⊕ Text Tokens]               │
│                        │                                    │
│              [统一 Transformer（所有模态共享权重）]             │
│                        │                                    │
│              [Image/Audio/Text 输出头]                       │
└─────────────────────────────────────────────────────────────┘
```

**Gemini 的多模态 Tokenizer 设计**：

```python
# Gemini 风格的统一 Tokenizer（概念示意）
class GeminiTokenizer:
    def __init__(self):
        self.text_tokenizer = SentencePiece(vocab_size=256_000)
        self.image_tokenizer = PatchTokenizer(patch_size=16, d_model=4096)
        self.audio_tokenizer = SpectrogramTokenizer(hop_ms=20, d_model=4096)
    
    def tokenize(self, inputs):
        tokens = []
        for item in inputs:
            if item.type == "text":
                tokens.extend(self.text_tokenizer.encode(item.content))
            elif item.type == "image":
                # 图像切成 patch，每个 patch 是一个 token
                tokens.extend(self.image_tokenizer.encode(item.content))
            elif item.type == "audio":
                # 音频切成帧，每帧是一个 token
                tokens.extend(self.audio_tokenizer.encode(item.content))
        return tokens  # 所有模态统一为 token 序列
```

**原生多模态的核心优势**：

1. **无模态边界**：模型内部没有"这是视觉 token，那是文本 token"的区分，融合最彻底
2. **多模态生成**：同一模型可以生成文本、图像、音频（理解和生成统一）
3. **长上下文多模态**：Gemini 1.5 Pro 的 10M token 上下文可以包含数小时视频
4. **涌现能力**：模态间的隐式对齐产生意想不到的跨模态推理能力

**代价**：训练成本极高，需要海量多模态数据，工程复杂度远超投影层方案。

### 5.7.4 主流理解模型横向对比（2026 Q1）

| 模型 | 架构 | 视觉编码器 | 参数量 | MMBench | MMStar | 特色 |
|------|------|-----------|:---:|:---:|:---:|------|
| GPT-4o | 原生多模态 | 未公开 | 未公开 | 89.4 | 67.5 | 端到端语音+视觉 |
| Gemini 2.0 Flash | 原生多模态 | NaViT | 未公开 | 88.7 | 66.2 | 10M 上下文 |
| Claude 3.7 Sonnet | 原生多模态 | 未公开 | 未公开 | 87.9 | 65.8 | 文档理解最强 |
| InternVL2-76B | 投影层 | InternViT-6B | 76B | 86.5 | 67.1 | 开源最强 |
| Qwen2.5-VL-72B | 投影层 | ViT-600M | 72B | 85.9 | 65.3 | 中文+文档 |
| LLaVA-OneVision-72B | 投影层 | SigLIP-SO400M | 72B | 85.1 | 61.7 | 视频理解强 |
| Llama 4 Scout | Early Fusion | SigLIP | 17B(active) | 83.2 | 60.4 | 开源 MoE |
| Gemma 4 27B | 原生多模态 | SigLIP-400M | 27B | 82.7 | 59.8 | 开源原生多模态 |

---

## 5.8 多模态生成模型：主流架构深度解析

多模态**生成**模型的核心任务是：根据文本/图像/视频描述，生成高质量的图像、视频、3D 内容。

### 5.8.1 图像生成：从 GAN 到扩散模型

**技术演进路线**：

```
2014: GAN（生成对抗网络）→ 模式崩溃，训练不稳定
2021: DALL-E 1（VQ-VAE + Transformer）→ 文生图概念验证
2022: DALL-E 2（CLIP + 扩散模型）→ 质量飞跃
2022: Stable Diffusion（开源扩散模型）→ 生态爆发
2023: SDXL（更大 UNet + 双文本编码器）→ 开源质量标杆
2024: SD3（DiT 架构 + Flow Matching）→ 架构革命
2025: FLUX.1（DiT + 双流 Transformer）→ 当前开源最强
2026: 统一理解+生成模型（Janus-Pro/Show-o）→ 融合趋势
```

### 5.8.2 扩散模型核心原理

扩散模型（Diffusion Model）的核心思想：**学习逆转加噪过程**。

**前向过程（加噪）**：

$$q(x_t | x_{t-1}) = \mathcal{N}(x_t; \sqrt{1-\beta_t} x_{t-1}, \beta_t I)$$

经过 T 步后，原始图像 $x_0$ 变为纯高斯噪声 $x_T \sim \mathcal{N}(0, I)$。

**反向过程（去噪，模型学习的部分）**：

$$p_\theta(x_{t-1} | x_t) = \mathcal{N}(x_{t-1}; \mu_\theta(x_t, t), \Sigma_\theta(x_t, t))$$

模型 $\epsilon_\theta$ 学习预测每一步加入的噪声：

$$\mathcal{L} = \mathbb{E}_{x_0, \epsilon, t} \left[ \| \epsilon - \epsilon_\theta(x_t, t, c) \|^2 \right]$$

其中 $c$ 是条件（文本 embedding），通过**交叉注意力**注入去噪网络。

```python
# 扩散模型推理（DDIM 采样，简化版）
def ddim_sample(model, text_prompt, steps=50, guidance_scale=7.5):
    # 文本编码
    text_emb = clip_encoder(text_prompt)        # [1, 77, 768]
    uncond_emb = clip_encoder("")               # 无条件 embedding
    
    # 从纯噪声开始
    x = torch.randn(1, 4, 64, 64)  # 潜空间噪声（VAE 压缩后）
    
    for t in reversed(range(steps)):
        # Classifier-Free Guidance: 有条件 - 无条件，放大差异
        noise_cond   = model(x, t, text_emb)
        noise_uncond = model(x, t, uncond_emb)
        noise_pred = noise_uncond + guidance_scale * (noise_cond - noise_uncond)
        
        # DDIM 去噪步骤
        x = ddim_step(x, noise_pred, t)
    
    # VAE 解码：潜空间 → 像素空间
    image = vae_decoder(x)
    return image
```

### 5.8.3 UNet vs DiT：去噪网络的架构革命

**UNet 架构（SD 1.x / SDXL）**：

```
输入噪声 [B, 4, H, W]
    │
[Conv 下采样] × 4 → 特征图逐渐缩小
    │
[中间 Transformer 块]（交叉注意力注入文本）
    │
[Conv 上采样] × 4 → 特征图逐渐恢复（Skip Connection）
    │
输出噪声预测 [B, 4, H, W]
```

**DiT 架构（SD3 / FLUX.1 / Sora）**：

DiT（Diffusion Transformer）用纯 Transformer 替代 UNet，将图像 patch 化后作为序列处理：

```python
class DiTBlock(nn.Module):
    """DiT 的核心块：自注意力 + 交叉注意力 + FFN"""
    def __init__(self, d_model, n_heads):
        super().__init__()
        self.self_attn = nn.MultiheadAttention(d_model, n_heads)
        self.cross_attn = nn.MultiheadAttention(d_model, n_heads)  # 注入文本
        self.ffn = FeedForward(d_model)
        # adaLN-Zero：时间步 t 和条件 c 通过 AdaLN 注入
        self.adaLN = AdaptiveLayerNorm(d_model)
    
    def forward(self, x, t_emb, text_emb):
        # x: 图像 patch token [B, N_patches, D]
        # t_emb: 时间步 embedding [B, D]
        # text_emb: 文本 embedding [B, L_text, D]
        
        # AdaLN：用时间步调制归一化参数
        x = self.adaLN(x, t_emb)
        
        # 自注意力（patch 之间互相关注）
        x = x + self.self_attn(x, x, x)[0]
        
        # 交叉注意力（patch 关注文本）
        x = x + self.cross_attn(x, text_emb, text_emb)[0]
        
        # FFN
        x = x + self.ffn(x)
        return x
```

**UNet vs DiT 对比**：

| 维度 | UNet（SDXL） | DiT（SD3/FLUX） |
|------|-------------|----------------|
| 架构 | CNN + Transformer 混合 | 纯 Transformer |
| 分辨率扩展 | 需要重新训练 | 天然支持任意分辨率 |
| 文本理解 | 交叉注意力（中等） | 双流 Transformer（更强） |
| 训练稳定性 | 较好 | 更好（Transformer 成熟） |
| 推理速度 | 快 | 稍慢（参数更多） |
| 图像质量 | 好 | 更好（SD3/FLUX 超越 SDXL） |

### 5.8.4 FLUX.1：当前开源图像生成最强

FLUX.1（Black Forest Labs，2024）是目前开源图像生成的质量标杆：

**核心创新**：

1. **双流 Transformer（MM-DiT）**：文本和图像 token 分别在各自的流中处理，通过交叉注意力交互，而非共享权重

```python
class MMDiTBlock(nn.Module):
    """FLUX.1 的双流 Transformer 块"""
    def __init__(self, d_model, n_heads):
        super().__init__()
        # 图像流
        self.img_self_attn = nn.MultiheadAttention(d_model, n_heads)
        self.img_ffn = FeedForward(d_model)
        # 文本流
        self.txt_self_attn = nn.MultiheadAttention(d_model, n_heads)
        self.txt_ffn = FeedForward(d_model)
        # 双流交叉注意力（图像↔文本）
        self.cross_attn = JointAttention(d_model, n_heads)
    
    def forward(self, img_tokens, txt_tokens, t_emb):
        # 双流联合注意力：图像和文本 token 一起做注意力
        img_tokens, txt_tokens = self.cross_attn(img_tokens, txt_tokens)
        
        # 各自的 FFN
        img_tokens = img_tokens + self.img_ffn(img_tokens)
        txt_tokens = txt_tokens + self.txt_ffn(txt_tokens)
        
        return img_tokens, txt_tokens
```

2. **Flow Matching 替代 DDPM**：更直接的训练目标，收敛更快

$$\mathcal{L}_{FM} = \mathbb{E}_{t, x_0, x_1} \left[ \| v_\theta(x_t, t) - (x_1 - x_0) \|^2 \right]$$

3. **三种变体**：FLUX.1-dev（开源研究）、FLUX.1-schnell（4步快速推理）、FLUX.1-pro（商业最强）

### 5.8.5 视频生成：从 Sora 到 Kling

视频生成在图像生成基础上增加了**时序维度**的建模。

**核心挑战**：
- 时序一致性：相邻帧之间的物体/场景要连贯
- 物理合理性：运动轨迹、光影变化要符合物理规律
- 长视频生成：内存和计算量随时长线性增长

**主流架构**：

```
Video DiT 架构（Sora/Kling/Veo 风格）:

视频帧 → [时空 Patch 化] → 时空 Token 序列
                                    │
                         [3D DiT（时间+空间联合注意力）]
                                    │
                         [文本条件（交叉注意力）]
                                    │
                         [时空 Token 序列] → [解码] → 视频帧
```

```python
# 视频 DiT 的时空注意力（简化）
class VideoSelfAttention(nn.Module):
    """时空联合注意力：同时建模空间和时间关系"""
    def __init__(self, d_model, n_heads):
        super().__init__()
        # 空间注意力：同一帧内的 patch 互相关注
        self.spatial_attn = nn.MultiheadAttention(d_model, n_heads)
        # 时间注意力：不同帧的同一位置 patch 互相关注
        self.temporal_attn = nn.MultiheadAttention(d_model, n_heads)
    
    def forward(self, x):
        # x: [B, T, H*W, D]（B批次, T帧, 空间patch, 维度）
        B, T, N, D = x.shape
        
        # 空间注意力：每帧内部
        x_spatial = x.reshape(B*T, N, D)
        x_spatial = self.spatial_attn(x_spatial, x_spatial, x_spatial)[0]
        x = x + x_spatial.reshape(B, T, N, D)
        
        # 时间注意力：跨帧同位置
        x_temporal = x.permute(0, 2, 1, 3).reshape(B*N, T, D)
        x_temporal = self.temporal_attn(x_temporal, x_temporal, x_temporal)[0]
        x = x + x_temporal.reshape(B, N, T, D).permute(0, 2, 1, 3)
        
        return x
```

**2026 年主流视频生成模型对比**：

| 模型 | 公司 | 最长时长 | 分辨率 | 架构 | 特色 |
|------|------|:---:|:---:|------|------|
| Sora | OpenAI | 60s | 1080p | Video DiT | 物理一致性最强（已关停公测） |
| Veo 3 | Google | 120s | 4K | Video DiT | 音视频同步生成 |
| Kling 2.0 | 快手 | 180s | 4K | Video DiT | 运动流畅度最优 |
| Pika 2.0 | Pika Labs | 30s | 1080p | DiT | 风格控制强 |
| CogVideoX | 智谱 | 60s | 720p | DiT | 开源最强 |
| Wan2.1 | 阿里 | 60s | 1080p | DiT | 开源，中文理解强 |

### 5.8.6 文本编码器：多模态生成的"大脑"

图像/视频生成质量的关键瓶颈之一是**文本理解能力**。

**演进路线**：

```
SD 1.x: CLIP ViT-L（77 token 限制，语义理解弱）
SDXL:   CLIP ViT-L + OpenCLIP ViT-bigG（双编码器，更丰富）
SD3:    CLIP + T5-XXL（110亿参数文本编码器，理解能力飞跃）
FLUX.1: CLIP + T5-XXL（同 SD3，但双流架构更好利用文本信息）
```

T5-XXL 的引入是关键转折点——它能理解复杂的空间关系、属性绑定、否定语义：

```python
# 为什么 T5-XXL 比 CLIP 更适合图像生成的文本理解
prompt = "一只红色的猫坐在蓝色的椅子上，旁边没有狗"

# CLIP 编码：77 token 限制，对"没有"等否定词理解弱
clip_emb = clip_encoder(prompt)  # [1, 77, 768]，语义压缩严重

# T5-XXL 编码：无 token 限制，双向注意力，理解更深
t5_emb = t5_encoder(prompt)  # [1, L, 4096]，保留完整语义
```

---

## 5.9 理解与生成的融合趋势

2025-2026 年最重要的趋势：**多模态理解和生成正在走向统一架构**。

### 5.9.1 为什么要统一？

传统方案的问题：

```
理解模型（LLaVA/InternVL）:  只能理解图像，不能生成
生成模型（SD/FLUX）:          只能生成图像，不能理解

用户需求: "理解这张图，然后生成一张类似但改变了某些元素的图"
→ 需要两个模型串联，信息损失大，延迟高
```

统一模型的优势：
1. **端到端**：理解和生成在同一模型内完成，无信息损失
2. **互相增强**：生成能力提升理解（通过生成验证理解），理解能力提升生成（更好的语义控制）
3. **更少参数**：一个模型替代两个

### 5.9.2 统一架构的三种路线

**路线 A：自回归统一（AR-based）**

代表：LlamaGen、Chameleon（Meta）、Janus-Pro（DeepSeek）

核心思路：将图像 token 化（VQ-VAE/VQGAN），与文本 token 一起做自回归预测。

```python
# Chameleon 风格：图像和文本 token 统一自回归
class ChameleonModel(nn.Module):
    def __init__(self):
        # 统一词表：文本 token + 图像 token
        self.vocab_size = 32000 + 8192  # 文本词表 + 图像码本
        self.embed = nn.Embedding(self.vocab_size, d_model)
        self.transformer = CausalTransformer()
        
        # 图像 tokenizer（VQ-VAE）
        self.image_tokenizer = VQVAE(codebook_size=8192)
    
    def generate_image(self, text_prompt):
        # 文本 token
        text_ids = tokenizer(text_prompt)
        
        # 自回归生成图像 token（256 个，对应 16×16 patch）
        image_ids = []
        for _ in range(256):
            logits = self.transformer(text_ids + image_ids)
            next_token = sample(logits[-1, 32000:])  # 只从图像词表采样
            image_ids.append(next_token + 32000)
        
        # 解码图像 token → 像素
        return self.image_tokenizer.decode(image_ids)
```

**Janus-Pro（DeepSeek，2025）的创新**：

Janus-Pro 发现理解和生成对视觉编码器的需求是**矛盾的**：
- 理解需要**高语义**的视觉特征（SigLIP 风格）
- 生成需要**高细节**的视觉特征（VQ-VAE 风格）

解决方案：**解耦视觉编码器**——理解和生成用不同的视觉编码器，但共享同一个 LLM 骨干：

```
理解路径: Image → [SigLIP Encoder] → [MLP] → LLM → 文本答案
生成路径: Text → LLM → [Image Token Head] → [VQ-VAE Decoder] → Image
```

**路线 B：扩散 + 自回归混合（Diffusion + AR）**

代表：Show-o（NUS，2024）、Transfusion（Meta，2024）

核心思路：文本用自回归（AR）生成，图像用扩散模型（Diffusion）生成，共享同一个 Transformer 骨干。

```python
# Transfusion 风格：AR + Diffusion 混合
class TransfusionModel(nn.Module):
    def __init__(self):
        self.transformer = SharedTransformer()
        self.text_head = nn.Linear(d_model, vocab_size)  # AR 文本生成
        self.diffusion_head = nn.Linear(d_model, patch_dim)  # 扩散图像生成
    
    def forward(self, tokens, mode):
        hidden = self.transformer(tokens)
        
        if mode == "text":
            # 自回归：预测下一个文本 token
            return self.text_head(hidden)
        elif mode == "image":
            # 扩散：预测噪声（去噪目标）
            return self.diffusion_head(hidden)
```

**路线 C：原生统一（Native Unified）**

代表：GPT-4o、Gemini 2.0、未来的 GPT-6

这是最彻底的统一——从预训练阶段就混合所有模态的理解和生成数据，模型天然具备所有能力。

GPT-4o 的统一能力展示：
```
输入: 文字 + 图像 → 输出: 文字（理解）
输入: 文字 → 输出: 图像（生成）
输入: 语音 → 输出: 语音（端到端对话）
输入: 图像 + 文字 → 输出: 图像（图像编辑）
```

### 5.9.3 统一模型的关键技术挑战

**挑战 1：理解 vs 生成的训练目标冲突**

```
理解目标: 最大化 P(文本答案 | 图像, 问题)
生成目标: 最大化 P(图像 | 文本描述)

→ 两个目标的梯度方向可能冲突
→ 解决方案: 分阶段训练 + 任务特定学习率
```

**挑战 2：图像 Token 化的质量瓶颈**

自回归图像生成（AR）的质量受限于 VQ-VAE 的码本大小：
- 码本太小：图像细节丢失
- 码本太大：序列太长，自回归效率低

2026 年的解决方案：**连续 token + 扩散头**（Transfusion 路线），绕过离散化的质量损失。

**挑战 3：多模态对齐数据稀缺**

高质量的"图像+文本+生成标注"三元组数据极其稀缺，是统一模型训练的核心瓶颈。

### 5.9.4 融合趋势全景图（2024-2026）

```
                    理解能力
                        ↑
                        │
    InternVL2-76B ●     │     ● GPT-4o
    Qwen2.5-VL-72B ●    │     ● Gemini 2.0
                        │
    LLaVA-OV ●          │     ● Janus-Pro
                        │
    ────────────────────┼────────────────────→ 生成能力
                        │
    FLUX.1 ●            │     ● Show-o
    SD3 ●               │     ● Transfusion
                        │
    Kling 2.0 ●         │     ● Veo 3
                        │
                        ↓

目标: 右上角（理解+生成都强）= GPT-4o / Gemini 2.0 / 未来 GPT-6
```

### 5.9.5 2026 年融合趋势总结

| 趋势 | 代表进展 | 影响 |
|------|---------|------|
| **理解+生成统一** | Janus-Pro, Show-o, GPT-4o | 一个模型替代理解+生成两个模型 |
| **视频理解+生成统一** | Veo 3（音视频同步）, Kling 2.0 | 视频内容创作门槛大幅降低 |
| **3D 理解+生成** | Hunyuan3D 2.1, Point-E 2.0 | 游戏/机器人行业受益 |
| **原生多模态 LLM** | Gemma 4, Llama 4 | 开源原生多模态普及 |
| **端侧多模态** | Gemma 4 E2B, Apple Intelligence | 手机本地多模态推理 |
| **世界模型** | Sora, Genie 2 | 物理世界建模，自动驾驶/机器人 |

---

## 5.10 前沿：统一多模态生成

下一代多模态模型的趋势是**理解和生成统一**：

### 统一生成架构

```python
class UnifiedMultimodalModel:
    """理解+生成统一架构（Janus-Pro 风格）"""
    
    def __init__(self):
        # 理解编码器（已有模态→token）
        self.vision_encoder = SigLIP()
        self.audio_encoder = USM()
        
        # 生成解码器（token→新模态）
        self.image_decoder = DiffusionHead()
        self.audio_decoder = VocoderHead()
        
        # 统一 Transformer 骨干
        self.backbone = TransformerDecoder(
            hidden_dim=4096,
            num_layers=32,
            num_heads=32
        )
    
    def forward(self, inputs, task):
        # 编码所有输入模态
        tokens = self.encode_all_modalities(inputs)
        
        # Transformer 处理
        hidden = self.backbone(tokens)
        
        # 根据任务选择输出模态
        if task == "text_generation":
            return self.text_head(hidden)
        elif task == "image_generation":
            return self.image_decoder(hidden)
        elif task == "speech_synthesis":
            return self.audio_decoder(hidden)
```

### 2026 年多模态生成全景

- **理解+生成统一**: Janus-Pro, Gemini 3.1, GPT-4o
- **视频理解与生成**: Sora（已关停）→ Kling 2.0, Pika 2.0, Veo 3
- **3D 生成**: Point-E 2.0, DreamFusion++
- **科学多模态**: AlphaFold 3（蛋白质+药物设计）

## 5.11 小结

多模态是 LLM 发展的核心方向：

| 模态 | 2024 水平 | 2026 水平 | 未来方向 |
|------|----------|----------|---------|
| **视觉理解** | 编码器+投影（LLaVA） | 原生多模态（Gemma 4/GPT-4o） | 统一理解+生成 |
| **图像生成** | UNet 扩散（SDXL） | DiT+Flow Matching（FLUX.1） | AR+扩散统一 |
| **视频生成** | 短视频（<10s） | 60-180s 高质量（Kling/Veo） | 实时视频推理 |
| **语音** | ASR→LLM→TTS | 端到端 300ms（GPT-4o） | 无 Tokenizer 150ms |
| **代码** | 单文件补全 | 自主编程 Agent（68%） | 端到端软件工程 |

**多模态理解三大架构流派**：
1. **投影层方案**（LLaVA/InternVL）：工程简单，开源生态最丰富
2. **交叉注意力**（Flamingo/Llama 4）：融合更深，多图/视频天然支持
3. **原生多模态**（GPT-4o/Gemini）：最彻底，但训练成本极高

**多模态生成核心趋势**：
1. **DiT 替代 UNet**：SD3/FLUX.1 证明纯 Transformer 架构在图像生成上全面超越 UNet
2. **Flow Matching 替代 DDPM**：更直接的训练目标，收敛更快，推理步数更少
3. **T5-XXL 文本编码器**：大幅提升文本理解能力，是 SD3/FLUX 质量飞跃的关键

**最重要的融合趋势**：
- 理解和生成正在走向统一架构（Janus-Pro/Show-o/GPT-4o）
- 自回归 + 扩散混合（Transfusion）是当前最有前景的统一路线
- 2026-2027 年将出现真正意义上的"全模态统一模型"

---

*本章由 Signal 知识平台 AI 智能体自动生成并持续修订。最后更新：2026-04-28*