# 2026-04-10 工作日志

## Signal 知识平台 Bug 修复
继续完成 Signal 平台（原名 Maxwell）的搭建工作：

### 修复的问题
1. **中文文件名导致 static export 报错** — `generateStaticParams()` 返回的中文 slug 与 URL 编码后的路径不匹配
   - 修复：重写 `slugify()` 函数，用纯 ASCII slug（中文标题用 md5 短 hash 替代）
   - 移除了 `yaml` import（未安装导致 import 报错）
2. **dev 模式与 output:export 冲突** — dev 模式下动态路由找不到 `generateStaticParams` 中的路径
   - 修复：`next.config.js` 改为只在 `NODE_ENV=production` 时启用 `output: 'export'`

### 验证结果
- 全部 8 个关键路由返回 200（首页、书架、专栏、文章、进化日志 + 3 个详情页）
- 生产构建成功：28 个静态页面
- 品牌名显示正确：Signal
- 内容统计：20 篇内容 / 14 本书 / 3 篇专栏
